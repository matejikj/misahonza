---
import { homepageData } from "../../data/homepage";
import ButtonLink from "../ui/ButtonLink.astro";
import SectionHeading from "../ui/SectionHeading.astro";

interface Props {
  faq: typeof homepageData.faq;
}

const { faq } = Astro.props;
---

<section id="faq" class="section-shell">
  <div class="container-page">
    <div class="grid gap-8 lg:grid-cols-12">
      <div class="lg:col-span-4">
        <SectionHeading title={faq.title} description={faq.intro} />
        <div class="mt-6">
          <ButtonLink href={faq.cta.href} label={faq.cta.label} variant="secondary" />
        </div>
        <div class="surface-panel mt-6 p-5">
          <p class="editorial-caption">Care guide</p>
          <h3 class="mt-3 text-base font-semibold">Jak pečovat o povrch po realizaci</h3>
          <ul class="mt-4 space-y-2 text-sm leading-6 text-text-muted">
            <li class="flex items-start gap-2">
              <span class="mt-[8px] h-1.5 w-1.5 rounded-full bg-accent"></span>
              Používej jemné čisticí prostředky bez abrazivních částic.
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-[8px] h-1.5 w-1.5 rounded-full bg-accent"></span>
              Silnější znečištění řeš co nejdříve, ideálně lokálně.
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-[8px] h-1.5 w-1.5 rounded-full bg-accent"></span>
              Po předání vždy dostaneš doporučení péče pro konkrétní typ povrchu.
            </li>
          </ul>
        </div>
      </div>

      <div class="lg:col-span-8 space-y-3">
        {faq.items.map((item) => (
          <details class="surface-panel group p-0">
            <summary class="focus-ring flex cursor-pointer list-none items-center justify-between gap-4 rounded-lg px-5 py-4 text-left text-sm font-semibold text-text">
              <span>{item.question}</span>
              <span class="inline-flex h-6 w-6 items-center justify-center rounded-full border hairline text-text-muted transition group-open:rotate-45">
                +
              </span>
            </summary>
            <div class="px-5 pb-5">
              <p class="text-sm leading-6 text-text-muted">{item.answer}</p>
            </div>
          </details>
        ))}
      </div>
    </div>
  </div>
</section>
